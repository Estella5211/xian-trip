# 西安旅行手册 · 傻瓜式部署指南（照着点就行）

> **你最后会得到什么**：一个公开的网址（例如 `https://xian-trip-handbook.xxx.workers.dev`），你和同行的人用手机点开就能看这本旅行手册，待办/打卡/花费还能**两人实时同步**。整套**完全免费**，大概 **15 分钟**搞定。
>
> **你只需要做 3 件事**：① 在 Cloudflare 网页上点几下、复制 3 串字符；② 用记事本改本文件夹里一个文件的 1 行；③ 在 GitHub 网页建个仓库，然后在弹出的黑框里粘贴 4 行命令。
> 不用装 Node、不用写代码、不用懂命令行。

---

## ✅ 开始前：准备 3 个免费账号（先注册好）

| # | 要注册 | 网址 | 备注 |
|---|---|---|---|
| ① | Cloudflare | https://dash.cloudflare.com/sign-up | 邮箱注册，验证一下邮箱 |
| ② | GitHub | https://github.com/signup | 用户名、密码、邮箱 |
| ③ | Git 工具 | https://git-scm.com/downloads | 装完一路下一步即可（Windows 会多一个"Git Bash"右键菜单） |

> 装 Git 时如果让你选「Adjusting your PATH」，选默认的 **"Git from the command line and also from 3rd-party software"** 一路 Next 就行，不用纠结。

---

## 🗺️ 心里有个数（总共 4 步）

```
第1步  Cloudflare 网页 → 建 Worker + 建 KV + 复制 3 串字符（ID / Account ID / Token）
第2步  记事本 → 改 wrangler.toml 里 1 行（把 KV 的 ID 填进去）
第3步  GitHub 网页 → 建仓库；Git Bash → 粘贴 4 行命令把文件传上去
第4步  GitHub 网页 → 填 2 个密码（Secrets）→ 自动部署 → 拿网址
```

下面每一步都写了「**点哪里 / 填什么 / 你会看到 / 卡住看这里**」。

---

## 第 1 步：Cloudflare 网页操作（复制 3 串字符）

### 1.1 登录
- 打开 https://dash.cloudflare.com 登录。

### 1.2 建一个 Worker（先建个空壳）
1. 左边栏点 **Workers & Pages**（如果没看到，点左上角三横菜单找）。
2. 右上角点 **Create**（或 **Create application**）→ 选 **Workers** 标签 → **Create Worker**。
3. 名字那栏填 `xian-trip-handbook`（随便填也行，记得住就好）→ 点 **Deploy**（或 Create）。
4. **不用写代码**，直接关掉这个编辑页面（点左上角返回即可）。
- 📌 你会看到：列表里多了一个 `xian-trip-handbook`。这就够了。

### 1.3 建 KV 存储（待办/花费同步靠它）
1. 左边栏 **Workers & Pages** → 往下找点 **KV**（也可能叫 **Workers KV** 或藏在 **Storage** 里）。
2. 点 **Create a namespace**（创建命名空间）。
3. 名字填 `trip-kv` → **Add**（或 Create）。
4. **复制它的 ID**：列表里点进 `trip-kv`，顶部有一串像 `a1b2c3d4...` 的字符，那就是 **KV ID**，先复制到记事本暂存。
- 📌 卡住看这里：找不到 KV 入口？在 Cloudflare 首页左侧菜单最下面点 **Workers & Pages**，进去后顶部有一排标签，找 **KV**。

### 1.4 复制 Account ID（账户 ID）
- 随便点进任意一个 Worker 页面，右侧边栏有一段 **Account ID**：`xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`，复制它，也存到记事本。
- （或者左下角点你账号名 → 右侧也有 Account ID。）

### 1.5 生成 API Token（让 GitHub 能自动帮你部署）

> ⭐ **推荐：用这条网址一键预填权限**（省得在几百个选项里找）。把下面整行复制到浏览器地址栏回车：
>
> ```
> https://dash.cloudflare.com/profile/api-tokens?permissionGroupKeys=%5B%7B%22key%22%3A%22workers_scripts%22%2C%22type%22%3A%22edit%22%7D%2C%7B%22key%22%3A%22workers_kv_storage%22%2C%22type%22%3A%22edit%22%7D%5D&accountId=%2A&zoneId=all&name=Xian%20Trip%20Deploy
> ```
>
> 打开后权限已经自动填好（Workers Scripts: Edit + Workers KV Storage: Edit），你只需要：确认 **Account Resources** 选了你的账号（或 All accounts）→ 底部 **Continue to summary** → **Create Token** → **复制 Token**。
> ⚠️ 一定要在 **Account Resources** 里把账号选上（点开下拉选你的账号，或选 All accounts），否则点 Continue 会没反应。

**或者手动创建（了解原理）：**
1. 右上角点你的**头像** → **My Profile**（个人资料）→ 左边 **API Tokens** → **Create Token**（创建令牌）。
2. 选 **Create Custom Token**（创建自定义令牌）。
3. 加两条权限，**名字要一字不差**：
   - `Account` → **`Workers Scripts`** → `Edit` ← ⚠️ 就填 `Workers Scripts`，**别加 "Cloudflare" 前缀**，否则搜不到
   - `Account` → **`Workers KV Storage`** → `Edit`
   > 💡 在下拉框里输入 `Workers` 会自动筛选，再从中选 `Workers Scripts`。
4. **Account Resources** 选 **Include → All accounts**（或你的账号）。
5. 底部 **Continue to summary** → **Create Token** → **复制 Token**。
6. **这串 Token 只显示一次！** 存到记事本，最好也发自己微信留底。

- 📌 卡住看这里：Token 漏了没关系，重做 1.5 再生成一个新的，把 GitHub 里的旧 Secret 覆盖掉即可。

> ✅ 第 1 步结束，你记事本里应有 3 样：
> - **KV ID**（1.3）
> - **Account ID**（1.4）
> - **API Token**（1.5）

---

## 第 2 步：用记事本改 1 个文件（填 KV ID）

1. 在本文件夹里找到 `wrangler.toml`，**右键 → 打开方式 → 记事本**。
2. 你会看到一行：
   ```toml
   id = "REPLACE_WITH_YOUR_KV_ID"
   ```
3. 把 `REPLACE_WITH_YOUR_KV_ID` 换成你 1.3 复制的 **KV ID**，保存关掉。
   - 改完长这样（举例）：
   ```toml
   [[kv_namespaces]]
   binding = "TRIP_KV"
   id = "a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4"
   ```
- ⚠️ **别动别的行**，也**别把 Account ID 写进这个文件**（它会在第 4 步用别的方式给 GitHub）。
- 📌 卡住看这里：找不到文件？确认你在项目根目录（和 `public` 文件夹同一层）。

---

## 第 3 步：GitHub 建仓库 + 把文件传上去

### 3.1 网页建仓库
1. 登录 https://github.com → 右上角 **+** → **New repository**（新建仓库）。
2. Repository name 填 `xian-trip`（随意）。
3. **不要**勾 "Add a README file"（别勾，否则后面会冲突）。
4. 点 **Create repository**（创建仓库）。
5. 创建后会看到一个快速上手页面，**不用管它**，关掉。

### 3.2 打开命令行，并确认「进对文件夹了」
- 在本文件夹里（和 `public`、`worker.js` 同一层）的空白处，**按住 Shift 右键 → 选择"在终端中打开"**（或"在此处打开 PowerShell 窗口"）；右键菜单里有 **"Git Bash Here"** 也行。
- 弹出黑框后，**先跑这一行进到项目文件夹**（如果窗口打开时已经在里面，跑一下也无害）：

```powershell
cd "D:\Users\DELL\Desktop\其他\workbuddy\2026-09-07-15-12-24"
```

- 判断标准：提示符（`PS` 后面那串）**末尾必须带 `2026-09-07-15-12-24`**。想再确认一次，敲：

```powershell
git rev-parse --show-toplevel
```

打印出的路径里带 `2026-09-07-15-12-24` 就对了。

### 3.3 一行一行粘贴命令（⚠️ 一次只粘一行，粘完按一次回车）
> 先只把 `你的用户名` 换成你 GitHub 的用户名（网址里 `github.com/XXX` 那个 XXX）。

```bash
git remote add origin https://github.com/你的用户名/xian-trip.git
git add -A
git commit -m "init"
git push -u origin main
```

> ⚠️ **最容易踩的坑**：千万不要把好几行一次性粘进去，也不要在同一行里重复粘两遍。否则命令会粘成类似
> `git push -u origin maingit push -u origin main` 这种东西，报错 `src refspec maingit does not match`、`src refspec push does not match`。
> 看到这种**乱码拼在一起**的报错，就是粘重了——**重新只粘 `git push -u origin main` 这一行**即可。

- 📌 你会看到：命令跑一会，最后 `git push` 会**弹出 GitHub 登录窗口**（或让你在浏览器点 Authorize）。按提示登录授权即可。
- ⚠️ **卡住看这里**：
  - 报错 `src refspec xxx does not match`：两种可能——① 命令粘重了（见上面的提醒），重新只粘那一行；② 分支名不对，先执行 `git branch -M main` 再重新 `git push -u origin main`。
  - 如果 `git remote add` 报错说 `remote origin already exists`：**正常**，说明之前加过，跳过那行直接跑后面 3 行就行。
  - 如果 `git commit` 说 `nothing to commit, working tree clean`：**也正常**，说明文件早就提交过了，继续跑 `git push` 即可。
  - 如果让你输入用户名/密码：现在 GitHub 不支持密码登录，请用浏览器弹窗授权；或去 GitHub → Settings → Developer settings → Personal access tokens 生成一个 token 当密码用。

### 3.4 不会用命令行？用「网页拖拽上传」更省事（零命令）
如果你觉得黑框敲命令别扭，可以完全不用命令行——直接在 GitHub 网页上把文件拖上去，**一样会自动部署**。

1. 打开你的仓库网页：`https://github.com/你的用户名/xian-trip`（`你的用户名`换成你的，比如 `Estella5211`）。
2. 点绿色 **Code** 按钮旁边的 **Add file → Upload files**。
3. 把下面这些**直接拖进网页的虚线框**（一次拖一个文件夹或几个文件都行）：
   - `public` 文件夹（整个）
   - `worker.js`
   - `wrangler.toml`
   - `部署手册.md`
   - `.github` 文件夹（**整个**——里面有自动部署脚本，必须传上去，否则不会自动发布）
   - ❌ **千万别拖** `.git` 文件夹和 `node_modules` 文件夹（它们不在项目里，一般也不会显示）。
4. 拖完后页面底部会出现一个输入框，填一句说明（比如 `初始上传`），然后点 **Commit changes**（绿色按钮）。
5. 网页会自己把文件传上去并生成一个提交——**这就等于完成了命令行里的 add + commit + push**。
6. 传好后直接跳到第 4 步填 Secrets 即可。

> 💡 网页上传成功后，本地那个黑框里之前的报错就都不用管了，因为文件已经在 GitHub 上了。

---

## 第 4 步：填 2 个密码（Secrets）+ 自动部署

### 4.1 填 Secrets
1. 打开你的 GitHub 仓库网页 → 顶部 **Settings**（设置）→ 左边 **Secrets and variables** → **Actions**。
2. 点 **New repository secret**（新建仓库密码），加第 1 个：
   - Name：`CF_API_TOKEN`
   - Secret：粘贴你 1.5 复制的 **API Token**
   - 点 **Add secret**。
3. 再点 **New repository secret**，加第 2 个：
   - Name：`CF_ACCOUNT_ID`
   - Secret：粘贴你 1.4 复制的 **Account ID**
   - 点 **Add secret**。

### 4.2 等它自动部署
- 回到仓库顶部点 **Actions**（动作）标签，你会看到一条正在跑的任务 **Deploy to Cloudflare Workers**。
- 等 1～2 分钟，变成 **绿色 ✓**（成功）就部署好了。
- （如果报错红叉，看下方"常见问题"。）

---

## 🎉 完成！拿网址

1. 去 Cloudflare 网页 → **Workers & Pages** → 点 `xian-trip-handbook`。
2. 右侧有一行 `https://xian-trip-handbook.<子域>.workers.dev` —— **这就是公开网址**，复制发给同行的人。
3. **手机上当 App 用**：手机浏览器打开该网址 → 点分享/三个点 → **"添加到主屏幕"**。首次在线打开后，之后离线也能看（浏览器已缓存）。

---

## 🔄 以后想改内容怎么办

改完 `public/index.html` 或任何文件后，在命令行再粘贴这 3 行（把引号里换成这次改了啥）：

```bash
git add -A
git commit -m "更新行程"
git push
```

GitHub 会自动重新部署，网址不变，刷新就能看到新内容。这就是「你 push 我就上」。

---

## ❓ 常见问题（人话版）

- **Actions 红叉 / 部署失败**
  去仓库 **Actions** 点开那条任务看日志。最常见两种：
  1. `CF_API_TOKEN` 权限不对 → 重做第 1.5 步，确认选的是 **Edit Cloudflare Workers** 模板（含 Workers + KV 的 Edit）。
  2. KV ID 填错 → 回第 2 步核对 `wrangler.toml` 里的 `id`。
  3. Account ID 没填 → 回第 4.1 步确认 `CF_ACCOUNT_ID` 已加。

- **待办/打卡/花费两个人不同步**
  确认 `wrangler.toml` 的 KV `id` 正确，且 Worker 已成功部署（Actions 绿勾）。旧部署不会同步。两人都要用**同一个网址**、且在手册里用同一个旅行口令（`xian4d-7f3a-9c2e-2026`）加入。

- **网址打不开 / 显示空白**
  先确认 Actions 是绿勾；再确认网址拼对了（`.workers.dev` 别漏）。

- **离线打不开**
  必须在**有网时先打开过一次**页面，浏览器才会缓存；之后离线可看。

- **想换旅行口令**
  改 `public/index.html` 里的 `TRIP.pass` 值（两台手机保持一致），push 上线生效。

---

## ⏰ 现在就该抢票（不用等部署）

| 项目 | 抢票时间 | 备注 |
|---|---|---|
| 赳赳大秦 10-02 16:45 场 | **9-22(周二) 9:30** | 提前 10 天开抢，卡点抢 A/B 区 |
| 陕西历史博物馆 10-02 场 | **9-27(周日) 17:00** | 公众号提前 5 天，提前填好两人信息 |
| 大明宫宴 10-03 午膳 | **尽早（9-18 起可订）** | 建议第一排至尊席 |

---

## 📁 文件清单（你不用管，知道有这些就行）

| 文件 | 作用 |
|---|---|
| `public/index.html` | 旅行手册本体（单文件、零依赖、离线可用） |
| `worker.js` | 云端同步接口（托管页面 + 待办/花费） |
| `wrangler.toml` | Worker 配置（你只改了里面的 KV id 一行） |
| `.github/workflows/deploy.yml` | push 即自动部署的脚本 |
| `部署手册.md` | 就是本文件 |
